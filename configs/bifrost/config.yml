server:
  port: 4000
  host: 0.0.0.0

cache:
  type: redis
  redis:
    addr: "aios-redis.qpid2b.0001.use1.cache.amazonaws.com:6379"
    ttl: 3600
  semantic_similarity: 0.95

rate_limit:
  enabled: true
  requests_per_minute: 60

vault:
  enabled: true
  addr: "http://172.31.79.159:8200"

logging:
  level: info
  format: json

providers:
  openrouter:
    base_url: "https://openrouter.ai/api/v1"
  anthropic:
    base_url: "https://api.anthropic.com"
  openai:
    base_url: "https://api.openai.com/v1"
