server:
  port: 4000
  host: 0.0.0.0

cache:
  type: redis
  redis:
    addr: "redis:6379"
    ttl: 3600
  semantic_similarity: 0.95

rate_limit:
  enabled: true
  requests_per_minute: 60

vault:
  enabled: true
  addr: "http://10.80.0.20:8200"

logging:
  level: info
  format: json

providers:
  openrouter:
    base_url: "https://openrouter.ai/api/v1"
  anthropic:
    base_url: "https://api.anthropic.com"
  openai:
    base_url: "https://api.openai.com/v1"
