name: default
filters:
  - Alert.Remediation == true && Alert.GetScope() == "Ip"
decisions:
  - type: ban
    duration: 4h
notifications:
  - name: http_ban
    webhook: http://traefik:8080/api/v1/decision
