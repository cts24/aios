[server]
domain = grafana.aios.local
root_url = https://grafana.aios.local

[auth]
disable_login_form = false

[security]
admin_user = admin
admin_password = ${GRAFANA_ADMIN_PASSWORD}

[users]
default_theme = dark
